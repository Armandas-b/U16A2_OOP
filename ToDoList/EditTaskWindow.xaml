<Window x:Class="ToDoList.EditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Task" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Edit Task" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />

        <TextBlock Grid.Row="1" Text="Title:" Margin="0,10,0,0" />
        <TextBox x:Name="textBoxTitle" Grid.Row="1" Margin="0,5,0,0" />

        <TextBlock Grid.Row="2" Text="Due Date:" Margin="0,10,0,0" />
        <DatePicker x:Name="datePickerDueDate" Grid.Row="2" Margin="0,5,0,0" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click" />
            <Button Content="Save" Width="80" Margin="5" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
