<mah:MetroWindow x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="To-Do List" Height="400" Width="300">

    <mah:MetroWindow.Resources>

        <Style TargetType="mah:MetroWindow">
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
    </mah:MetroWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="My To-Do List" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        <ListBox Grid.Row="1" Margin="10" x:Name="listBoxTasks" ItemsSource="{Binding ListBoxTasks}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding TaskName}" Margin="5" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding DueDate, StringFormat='yyyy-MM-dd'}" Margin="5" VerticalAlignment="Center"/>

                        <Button Grid.Column="2" Margin="5" Width="30" Height="30" Command="{Binding EditCommand}">
                            <fa:FontAwesome Icon="Edit" Foreground="Black" Width="20" Height="20" />
                        </Button>

                        <Button Grid.Column="3" Margin="5" Width="30" Height="30" Command="{Binding DeleteCommand}">
                            <Image Source="delete_icon.png" Width="20" Height="20" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>



        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20">
          <!--  <Button Content="Add Task" Width="100" Margin="5" Click="AddTask_Click"/> -->
          <!--  <Button Content="Delete Task" Width="100" Margin="5" Click="DeleteTask_Click"/> -->
           <!-- <Button Content="Edit Task" Width="100" Margin="5" Click="EditTask_Click"/> -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Filter:" VerticalAlignment="Center"/>
                <ComboBox x:Name="filterComboBox" SelectedIndex="0" Width="150" Margin="5" SelectionChanged="filterComboBox_SelectionChanged">
                    <ComboBoxItem Content="All Tasks"/>
                    <ComboBoxItem Content="Completed Tasks"/>
                    <ComboBoxItem Content="Uncompleted Tasks"/>
                </ComboBox>
            </StackPanel>

        </StackPanel>
    </Grid>
</mah:MetroWindow>
