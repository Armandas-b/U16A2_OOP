<mah:MetroWindow x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="To-Do List" Height="400" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="My To-Do List" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        <ListBox x:Name="listBoxTasks" Grid.Row="1" Margin="10">
            
        </ListBox>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Add Task" Width="100" Margin="5" Click="AddTask_Click"/>
            <Button Content="Delete Task" Width="100" Margin="5" Click="DeleteTask_Click"/>
            <Button Content="Edit Task" Width="100" Margin="5" Click="EditTask_Click"/>
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Filter:" VerticalAlignment="Center"/>
                <ComboBox x:Name="filterComboBox" SelectedIndex="0" Width="150" Margin="5">
                    <ComboBoxItem Content="All Tasks"/>
                    <ComboBoxItem Content="Completed Tasks"/>
                    <ComboBoxItem Content="Uncompleted Tasks"/>
                </ComboBox>
                <Button Content="Apply Filter" Width="100" Margin="5" Click="ApplyFilter_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
